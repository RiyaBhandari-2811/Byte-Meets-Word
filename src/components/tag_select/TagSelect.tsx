// import { Autocomplete } from "@mui/material"
// import { Controller } from "react-hook-form"

const TagSelect = () => {
  return (
    // <Controller
    // name = "tags"
    // control={control}
    // render={() => (
    //     <Autocomplete
    //     multiple
    //     options={[]}

    //     />
    // )}

    // />
    <div>
      <p>Tag Select</p>
    </div>
  );
};

export default TagSelect;
